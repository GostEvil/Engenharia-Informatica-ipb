// Existe 3 soluções pare este exercício

// int X = 1, Sum = 0;
// while (X <= 11){
// 		Sum = Sum + X;
//		X = X + 1;
// }
// printf("%d", Sum);

// 1.ª Solução
// X <= 11 <=> 	X < 11 || X == 1
//				x - 11 < 0 || X - 11 == 0

// 2.ª Solução 
// X <= 11 => X < 11 + 1

// 3.ª Solução
// X <= 11 => X > 11